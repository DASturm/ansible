---
- hosts: all
  gather_facts: yes
  tasks:
    - name: Checking Repository Destinations
      file:
        path: ~/git/{{ hostvars[item].name }}
        state: directory
#      with_items: 
#        - "{{ hostvars[ansible_host]['gitconfig'] }}"

    - name: Loading and Updating Repositories
      git:
        repo: "{{ hhostvars[item].repo }}"
        dest: ~/git/{{ hostvars[item].name }}
        clone: yes
        update: yes
#      with_items: 
#        - "{{ hostvars[['gitconfig'] }}"
